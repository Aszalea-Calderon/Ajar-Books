allowBuilds:
  better-sqlite3: true
  esbuild: true

# cookie <0.7.0 has a known low-severity validation gap (GHSA-pxg6-pf52-xh8x);
# @sveltejs/kit 2.70.1 (latest stable) still bundles 0.6.0, so force the patch.
overrides:
  cookie: '^0.7.0'
